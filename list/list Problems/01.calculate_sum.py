def calculate_sum(numbers):
    total = sum(numbers)  # Use the built-in 'sum' function to calculate the sum of the numbers
    return total

# Example usage:
numbers = [3, 3, 6, 73, 1]
result = calculate_sum(numbers)
print("The sum of the numbers is:", result)
